
```markdown
# 🚀 免费部署指南：Railway.app

## 第一步：准备 GitHub 仓库

1. 创建一个新的 GitHub 仓库（如 `mental-health-bot`）
2. 推送本项目所有文件

```bash
git init
git add .
git commit -m "initial commit"
git remote add origin https://github.com/wly060805-hash/mental-health-bot.git
git branch -M main
git push -u origin main